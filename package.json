{
  "name": "vocabloop-api",
  "version": "1.0.0",
  "private": true,
  "description": "VocabLoop backend API (auth + sync)",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "@libsql/client": "^0.14.0",
    "express": "^4.21.0"
  }
}
